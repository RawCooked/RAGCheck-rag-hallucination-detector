# Copy this file to .env and fill in values as needed.
# Not required for Step 1 (local generation backend needs no key).

# Only needed if config.yaml -> generation.backend is switched to "openai".
OPENAI_API_KEY=

# Optional: override the HuggingFace cache location.
# HF_HOME=./models_cache
